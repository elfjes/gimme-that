[flake8]
max-line-length = 99

[tool:pytest]
testpaths = tests

[coverage:paths]
source =
   src
   .tox/*/site-packages

[coverage:run]
parallel = true
omit =
    tests/*

[coverage:report]
show_missing = true
